
  <div class="MainCardsArea">
    <%= form_for :account_transfer, url: {action: "create"} do |f| %>

    <%#= form_with(url: new_social_account_account_transfer_path, local: true) do |form| %>
      <% if account_transfer.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(account_transfer.errors.count, "error") %> prohibited this account_transfer from being saved:</h2>

          <ul>
          <% account_transfer.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="receiver">
        <h3>Получатели</h3>
        <%= f.email_field :email, placeholder: "Введите почту получателя..." %>
      </div>

      <div class="receiver">
        <h3>Inheritor</h3>
        <%= f.text_field :inheritor_id, placeholder: "Введите почту получателя..." %>
      </div>

      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
  </div>
