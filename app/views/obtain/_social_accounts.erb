<% @inherited_accounts.each do |i| %>
  <% social_account = i.transferable %>
  <% if i.transferable.class == SocialAccount %>

    <% if i.accepted %>
      <div class="card">
        <div class="previev <%= social_account.title %>"></div>
        <div class="card_title">
          <h4><%= social_account.title %></h4>
          <p class="transmitter_email"><%= social_account.email %></p>
        </div>

        <div class="editing">
          <div class="popup">

              <div class="UnacceptWrapper">
                <div class="Icon" id="UnacceptedIcon"></div>
                <li>
                  <%= link_to 'Отклонить', unaccept_social_account_account_transfer_path(social_account), method: :delete, data: { confirm: 'Are you sure?'} %>
                </li>
              </div>
          </div>
        </div>
      </div>
    <% else %>
      <div class="card">
        <div class="previev_unaccepted"></div>
        <div class="previev <%= social_account.title %>"></div>
        <div class="card_title">
          <h4><%= social_account.title %></h4>
          <p class="transmitter_email"><%= social_account.email %></p>
        </div>

        <div class="editing">
          <div class="popup">
              <div class="AcceptWrapper">
                <div class="Icon" id="AcceptedIcon"></div>
                <li>
                  <%= link_to 'Принять', accept_social_account_account_transfer_path(social_account), method: :put %>
                </li>
              </div>
              <div class="UnacceptWrapper">
                <div class="Icon" id="UnacceptedIcon"></div>
                <li>
                  <%= link_to 'Отклонить', unaccept_social_account_account_transfer_path(social_account), method: :delete, data: { confirm: 'Are you sure?'} %>
                </li>
              </div>
          </div>
        </div>
      </div>

    <% end %>
  <% end %>
<% end %>
